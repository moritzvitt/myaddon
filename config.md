# Configuration

## deck

Default deck name shown in configuration and preview-related workflows.

## preview_query

Search query used by the startup preview tagging workflow.

## preview_tag

Tag applied to the preview note set during startup.

## preview_limit

Maximum number of preview cards considered when generating the preview tag.
